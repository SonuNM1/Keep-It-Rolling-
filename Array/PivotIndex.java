/*
A pivot index is an index in the array where the sum of all elements on its left is equal to the sum of all elements on its right. 

The element at the pivot index itself is not included in either sum. If multiple pivot indices exist, we return the leftmost one; if none exist, we return -1.
*/

public class PivotIndex {
    public static int pivot(int[] arr){
        int index = -1 ; 
        int leftSum = 0, rightSum = 0; 

        for(int i=1 ; i < arr.length; i++){

        }

        return index ; 
    }
    public static void main(String[] args) {
        
    }
}
